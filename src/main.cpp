#include <iostream>

#include "Chip8.h"

int main() {
    std::cout << sizeof(uint8_t);
    std::cout << sizeof(uint16_t);
    std::cout << sizeof(uint32_t);
    std::cout << sizeof(uint64_t);

    return 0;
}
